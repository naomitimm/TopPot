import '../../exports.dart';

class AuthButton1 extends StatelessWidget {
  const AuthButton1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
